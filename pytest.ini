[pytest]
addopts = -v --html=reports/reporte.html --self-contained-html
testpaths = tests
markers =
    historia(id): identifica la historia de usuario cubierta
    tipo(nombre): clasifica el escenario como feliz, negativo o limite

